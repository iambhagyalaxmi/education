const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const ann = await prisma.announcement.create({ data: { title: 'Test', audience: 'All', content: 'Test content', status: 'Draft' } }); console.log('Created:', ann.id); const all = await prisma.announcement.findMany(); console.log('Count:', all.length); } catch(e) { console.error('Error:', e); } finally { await prisma.$disconnect(); } } main();
