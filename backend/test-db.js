const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const res = await prisma.subject.findMany(); console.log('Subjects:', res.length); } catch (e) { console.error('Error:', e); } } main();
