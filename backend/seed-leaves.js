const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const users = await prisma.user.findMany({ take: 3 }); if (users.length === 0) return console.log('No users found.'); await prisma.leaveRequest.createMany({ data: [ { userId: users[0].id, type: 'Annual Leave', duration: '3 days', reason: 'Family vacation', status: 'Pending' }, { userId: users[0].id, type: 'Sick Leave', duration: '1 day', reason: 'Fever and cold', status: 'Approved' }, { userId: users[1]?.id || users[0].id, type: 'Unpaid Leave', duration: '5 days', reason: 'Personal matters', status: 'Pending' } ] }); console.log('Successfully seeded 3 leave requests!'); } catch(e) { console.error(e); } } main();
