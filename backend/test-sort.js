const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const subjects = await prisma.subject.findMany({ include: { course: true }, orderBy: { semester: 'asc' } }); subjects.sort((a, b) => (a.course?.name || '').localeCompare(b.course?.name || '')); console.log('Sorted:', subjects.length); } catch(e) { console.error('Error:', e); } } main();
